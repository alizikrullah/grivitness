{
  "compilerOptions": {
    // Node 20+ mendukung penuh ES2023
    "target": "ES2023",
    "lib": ["ES2023"],

    // WAJIB NodeNext karena package.json pakai "type": "module".
    // Konsekuensinya: semua relative import harus berekstensi .js
    // contoh: import { env } from './config/env.js'
    "module": "NodeNext",
    "moduleResolution": "NodeNext",
    "moduleDetection": "force",

    // Type-only import wajib ditulis eksplisit `import type { ... }`.
    // Mencegah import yang cuma dipakai sebagai tipe ikut ter-emit ke runtime.
    "verbatimModuleSyntax": true,

    "strict": true,
    "noUncheckedIndexedAccess": true,
    "noImplicitOverride": true,
    "noFallthroughCasesInSwitch": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,

    "esModuleInterop": true,
    "resolveJsonModule": true,
    "forceConsistentCasingInFileNames": true,
    "skipLibCheck": true,

    "sourceMap": true,
    "noEmit": true,
    "types": ["node"]
  },
  // tests/ dan vitest.config.ts ikut di sini supaya ESLint bisa menjalankan
  // typed linting padanya. Keduanya dikecualikan dari tsconfig.build.json,
  // jadi tidak ikut masuk artefak production.
  "include": ["src/**/*.ts", "scripts/**/*.ts", "tests/**/*.ts", "vitest.config.ts"],
  "exclude": ["node_modules", "dist"]
}
